import RevealWrapper from "./RevealWrapper";

export default function Payment() {
  return (
    <RevealWrapper className="relative z-10 mt-[38px] px-5 pb-[104px]">
      <div className="font-bricolage font-extrabold text-[28px] leading-[1.12] text-marca-escura tracking-[-0.03em] text-center">
        Aceitamos Pix e cartão de crédito
      </div>
    </RevealWrapper>
  );
}
